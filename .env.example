# Copia este archivo a .env.local (nunca subas .env a git)

# URL base de la API en produccion (sin /api/v1, el cliente lo agrega).
# En desarrollo deja vacio: Vite usa el proxy a http://127.0.0.1:5000
VITE_API_URL=https://tu-backend.vercel.app
