import { StatisticsBar } from '../components/StatisticsBar';

export default function HomePage() {
  return (
    <div className="max-w-7xl mx-auto w-full min-w-0 min-h-full" >
      <StatisticsBar />
    </div>
  );
}
